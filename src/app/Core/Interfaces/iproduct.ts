export interface Product {
  id: number;
  title: string;
  category: string;
  categoryLabel: string;
  imgUrl: string;
  price: number;
  originalPrice: number;
  rating: number;
  reviews: number;
  onSale: boolean;
}
